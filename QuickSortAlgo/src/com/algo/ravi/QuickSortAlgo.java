package com.algo.ravi;

/**
 * Quick has two important methods.
 * 1. The "partition" method which finds a pivot and moves elements to before or after the pivot
 * 2. The "QuickSort" method which does the recursive call to sort the sub-lists.
 * 
 * @author chandu
 */
public class QuickSortAlgo {
	
	public void partition(int[] ar, int low, int high) {
		int pivot = low;
		int l = low;
		int h = high;
		
		while(l < h) {
			
		}
	}
	
	public static void main(String[] args) {

	}

}
